// Marcador de build — BUMPAR a cada alteração para confirmarmos, no app rodando,
// que o código novo está de fato carregado (evita confusão com instâncias antigas).
// Aparece na sidebar e no log de boot do main.
export const BUILD_TAG = "v2.0";
